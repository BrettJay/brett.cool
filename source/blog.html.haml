---
  title: Blog
  body_classes: blog
  meta_description: Occasional blog posts
  meta_keywords: Design, development, blog
---

.Page
  .Content
    - page_articles.each_with_index do |article, i|
      .Row.pb3
        .Content_Main
          %h2.h1_scale= link_to article.title, article
          .Article_metadata
            %span.metadata_date= Date.parse(article.date.to_s).strftime('%b %e, %Y')
            ·
            %span.metadata_reading_time #{time_label article.body}
      = article.summary
      .Row.pb4.pt2
        .Content_Main.Content_Article
          = link_to article, class: 'read-more' do
            Continue Reading
